version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Copy env
          command: cp .test.example .env 
      - run:
          name: Run tests
          command: make test
